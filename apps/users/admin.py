"""Users 管理后台注册模块。

用于在 Django 管理后台中注册用户相关模型（如用户资料、关系等）。
后续在 models.py 定义模型后，可通过 admin.site.register 进行注册。
"""

from django.contrib import admin

# 示例：
# from .models import UserProfile
# admin.site.register(UserProfile)
